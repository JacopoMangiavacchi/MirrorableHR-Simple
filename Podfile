# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MirrorableHR' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MirrorableHR
  pod 'AppCenter'
  
  target 'MirrorableHRTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'MirrorableHR WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MirrorableHR WatchKit App

end

target 'MirrorableHR WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MirrorableHR WatchKit Extension

end
